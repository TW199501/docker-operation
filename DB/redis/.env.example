# =============================================================================
# Redis 環境變數範本
# 規範:docs/conventions/env-naming.md
# 用法:./scripts/env/bootstrap-env.sh DB/redis
# =============================================================================

REDIS_PASSWORD=$(openssl rand -base64 32)      # 自動生成
