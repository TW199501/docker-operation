version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: always
    ports:
      - "9379:6379"
    command: ["redis-server", "--requirepass", "your_password"]  
    volumes:
      - ./redis_data:/data