# =============================================================================
# Electerm Web 環境變數範本
# 規範:docs/conventions/env-naming.md
# 用法:./scripts/env/bootstrap-env.sh electerm-web
# =============================================================================

SERVER_SECRET=$(openssl rand -hex 32)          # 自動生成
SERVER_PASS=$(openssl rand -base64 24)         # 自動生成
